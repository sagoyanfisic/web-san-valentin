---
// src/components/CevicheInvitation.astro
---

<style is:global>
  :root {
    --color-primary: #ff6b6b;
    --color-secondary: #4ecdc4;
    --color-accent: #ffe66d;
  }

  @keyframes float {
    0% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(3deg); }
    100% { transform: translateY(0px) rotate(0deg); }
  }

  @keyframes star-rotate {
    0% { transform: rotate(0deg) scale(1); }
    100% { transform: rotate(360deg) scale(1.2); }
  }

  @keyframes confetti {
    0% { transform: translateY(-100vh) rotate(0deg); opacity: 1; }
    100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
  }

  @keyframes title-bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-20px); }
  }

  @keyframes button-dance {
    0%, 100% { transform: rotate(0deg); }
    25% { transform: rotate(3deg); }
    75% { transform: rotate(-3deg); }
  }

  .container {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background: linear-gradient(45deg, #ff758c, #ff7eb3);
    overflow: hidden;
    position: relative;
  }

  .ceviche-animation {
    width: 350px;
    height: 350px;
    border-radius: 50%;
    border: 4px solid #fff;
    box-shadow: 0 15px 35px rgba(0,0,0,0.2);
    animation: float 3s ease-in-out infinite;
    cursor: pointer;
    transition: transform 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .ceviche-animation:hover {
    transform: scale(1.05) rotate(5deg);
  }

  .ceviche-animation:active {
    animation: star-rotate 0.5s ease-in-out;
  }

  .title {
    font-size: 3.5rem;
    color: white;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    margin: 2rem 0;
    animation: title-bounce 2s ease-in-out infinite;
    font-family: 'Comic Neue', cursive;
  }

  .button-container {
    display: flex;
    gap: 2rem;
    margin-top: 2rem;
    perspective: 1000px;
  }

  .accept-btn {
    background: linear-gradient(45deg, #4ecdc4, #45b7af);
    padding: 1.5rem 3rem;
    border: none;
    border-radius: 30px;
    color: white;
    font-size: 1.5rem;
    cursor: pointer;
    transition: all 0.3s ease;
    transform-style: preserve-3d;
    position: relative;
    overflow: hidden;
    animation: button-dance 2s ease-in-out infinite;
  }

  .accept-btn:hover {
    transform: scale(1.1) rotate(2deg);
    box-shadow: 0 10px 20px rgba(0,0,0,0.2);
  }

  .accept-btn::after {
    content: '🐟';
    position: absolute;
    right: -20px;
    top: -20px;
    font-size: 2rem;
    opacity: 0;
    transition: all 0.3s ease;
  }

  .accept-btn:hover::after {
    opacity: 1;
    right: 10px;
    top: 10px;
  }

  .deny-btn {
    background: linear-gradient(45deg, #ff6b6b, #ff5252);
    padding: 1.5rem 3rem;
    border: none;
    border-radius: 30px;
    color: white;
    font-size: 1.5rem;
    cursor: not-allowed;
    opacity: 0.8;
    transition: all 0.3s ease;
    position: relative;
  }

  .deny-btn:hover {
    animation: shake 0.5s ease-in-out;
  }

  .stars {
    position: absolute;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }

  .star {
    position: absolute;
    background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="%23ffe66d"><path d="M12 0l3.09 6.26L22 7.27l-5 4.87 1.18 6.88L12 16l-6.18 3.02L7 12.14 2 7.27l6.91-1.01L12 0z"/></svg>');
    background-size: contain;
    animation: star-float 8s linear infinite;
  }

  @keyframes star-float {
    0% {
      transform: translateY(100vh) rotate(0deg);
      opacity: 0;
    }
    50% {
      opacity: 1;
    }
    100% {
      transform: translateY(-100vh) rotate(720deg);
      opacity: 0;
    }
  }

  .confetti {
    position: absolute;
    width: 10px;
    height: 10px;
    background: var(--color-accent);
    animation: confetti 3s linear forwards;
    opacity: 0;
  }

  @media (max-width: 768px) {
    .ceviche-animation {
      width: 250px;
      height: 250px;
    }
    
    .title {
      font-size: 2.5rem;
    }
  }
</style>

<div class="container">
  <!-- Estrellas flotantes de fondo -->
  <div class="stars">
    <div class="star" style="left: 10%; animation-delay: 0s"></div>
    <div class="star" style="left: 30%; animation-delay: 2s"></div>
    <div class="star" style="left: 50%; animation-delay: 4s"></div>
    <div class="star" style="left: 70%; animation-delay: 6s"></div>
    <div class="star" style="left: 90%; animation-delay: 8s"></div>
  </div>

  <img 
    src="https://images.unsplash.com/photo-1535399831218-d5bd36d1a6b3" 
    class="ceviche-animation"
    alt="Ceviche Peruano"
  />

  <h1 class="title">
    ¿Ceviche este <span style="color: var(--color-accent)">❤️ San Valentín</span>?
  </h1>

  <div class="button-container">
    <button class="accept-btn">¡Sí, vamos! 🎉</button>
    <button class="deny-btn">😢 No puedo</button>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const acceptBtn = document.querySelector('.accept-btn');
    const cevicheImage = document.querySelector('.ceviche-animation');
    
    // Efecto de confeti al aceptar
    acceptBtn.addEventListener('click', () => {
      for(let i = 0; i < 50; i++) {
        const confetti = document.createElement('div');
        confetti.className = 'confetti';
        confetti.style.left = Math.random() * 100 + '%';
        confetti.style.animationDelay = Math.random() * 1 + 's';
        confetti.style.backgroundColor = `hsl(${Math.random() * 360}, 70%, 60%)`;
        document.body.appendChild(confetti);
        
        setTimeout(() => confetti.remove(), 3000);
      }
      
      setTimeout(() => {
        alert('🎉 ¡Perfecto! Preparando mesa con vista al mar... 🏖️');
      }, 500);
    });

    // Efecto especial al tocar la imagen
    cevicheImage.addEventListener('click', () => {
      cevicheImage.style.animation = 'star-rotate 0.8s ease-in-out';
      setTimeout(() => {
        cevicheImage.style.animation = 'float 3s ease-in-out infinite';
      }, 800);
    });
  });
</script>